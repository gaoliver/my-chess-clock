import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const RulesetScreen = () => {
	return (
		<View>
			<Text>Ruleset Screen</Text>
		</View>
	)
}

export default RulesetScreen

const styles = StyleSheet.create({})
